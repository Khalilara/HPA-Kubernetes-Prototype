apiVersion: v1
kind: Service
metadata:
  name: hpa-cpu-app-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: hpa-cpu-app
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30000  # Port accessible sur le host
